import { ensureTreatmentThemeLibrary,treatmentThemeById } from './treatment-theme-library.js';
import { matchesTreatmentThemeSearch } from './treatment-theme-parser.js';

let enhancing=false;
function esc(value=''){return String(value).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));}
function normalize(value=''){return String(value).normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();}
function enhanceTreatmentForm(){const form=document.querySelector('#treatment-form');if(!form||form.dataset.themeLibraryEnhanced)return;const first=form.querySelector('[data-treatment-item]');if(!first)return;form.dataset.themeLibraryEnhanced='true';const panel=document.createElement('section');panel.className='treatment-theme-entry';panel.innerHTML='<div><p class="eyebrow">Tratamento por tema</p><strong>Comece de uma sugestão terapêutica</strong><span>Use os comandos da biblioteca original como ponto de partida e escolha os gráficos separadamente.</span></div><button type="button" class="btn secondary" data-open-treatment-theme>Ver sugestões</button>';first.before(panel);}
function enhance(){if(enhancing)return;enhancing=true;try{enhanceTreatmentForm();}finally{enhancing=false;}}
new MutationObserver(()=>queueMicrotask(enhance)).observe(document.body,{childList:true,subtree:true});queueMicrotask(enhance);
function renderPicker(items){const themes=[...new Set(items.map(item=>item.theme))],wrap=document.createElement('div');wrap.id='treatment-theme-overlay';wrap.className='modal-backdrop';wrap.innerHTML=`<section class="sheet treatment-theme-sheet"><div class="sheet-head"><div><p class="eyebrow">Tratamentos por tema</p><h2>Escolha um ponto de partida</h2><p class="muted">Sugestões terapêuticas. O tópico e o comando serão preenchidos; o gráfico continua sendo uma escolha separada.</p></div><button class="close-btn" data-close-treatment-theme>×</button></div><label class="field"><span>Buscar sugestão</span><input type="search" data-treatment-theme-search placeholder="Ex.: prosperidade, carreira, autoestima…"></label><div class="theme-chips"><button type="button" class="theme-chip active" data-treatment-theme-filter="">Todos</button>${themes.map(theme=>`<button type="button" class="theme-chip" data-treatment-theme-filter="${esc(theme)}">${esc(theme)}</button>`).join('')}</div><div class="treatment-theme-list">${items.map(item=>`<article class="treatment-theme-card" data-treatment-theme-card data-theme="${esc(item.theme)}" data-search="${esc(item.search||normalize(`${item.title} ${item.command} ${item.theme}`))}"><div><p class="eyebrow">${esc(item.theme)}</p><h3>${esc(item.title)}</h3><p>${esc(item.command)}</p></div><button type="button" class="btn secondary" data-apply-treatment-theme="${esc(item.id)}">Usar sugestão</button></article>`).join('')}</div><div class="empty" data-treatment-theme-empty hidden>Nenhuma sugestão encontrada.</div></section>`;document.body.appendChild(wrap);}
function filterPicker(overlay){const query=overlay.querySelector('[data-treatment-theme-search]')?.value||'',theme=overlay.querySelector('[data-treatment-theme-filter].active')?.dataset.treatmentThemeFilter||'';let visible=0;overlay.querySelectorAll('[data-treatment-theme-card]').forEach(card=>{const show=matchesTreatmentThemeSearch(card.dataset.search||'',query)&&(!theme||card.dataset.theme===theme);card.hidden=!show;if(show)visible++;});overlay.querySelector('[data-treatment-theme-empty]')?.toggleAttribute('hidden',visible>0);}
function targetItem(form){let items=[...form.querySelectorAll('[data-treatment-items] > [data-treatment-item]')],item=items.find(row=>!row.querySelector('[name="itemLabel"]')?.value&&!row.querySelector('[name="commandText"]')?.value);if(item)return item;form.querySelector('[data-add-treatment-item]')?.click();items=[...form.querySelectorAll('[data-treatment-items] > [data-treatment-item]')];return items.at(-1)||null;}
function applySuggestion(item){
  const form=document.querySelector('#treatment-form');if(!form||!item)return;const title=form.querySelector('[name="title"]');if(title&&!title.value)title.value=item.title;
  const section=targetItem(form);if(!section)return;const itemLabel=section.querySelector('[name="itemLabel"]'),command=section.querySelector('[name="commandText"]');if(itemLabel)itemLabel.value=item.title;if(command)command.value=item.command;
  section.dataset.treatmentTheme=item.theme;section.dataset.treatmentThemeSource=item.sourcePath;section.dataset.treatmentThemeSuggestion=item.id;
  form.dataset.treatmentTheme=item.theme;form.dataset.treatmentThemeSource=item.sourcePath;form.dataset.treatmentThemeSuggestion=item.id;
  let notice=form.querySelector('.treatment-theme-applied');if(!notice){notice=document.createElement('div');notice.className='notice treatment-theme-applied';form.prepend(notice);}notice.innerHTML=`<strong>Sugestão aplicada · ${esc(item.theme)}</strong><span>A origem fica preservada neste item. Revise o tópico e o comando; depois escolha ao menos um gráfico.</span>`;section.scrollIntoView({behavior:'smooth',block:'center'});
}
document.addEventListener('input',event=>{if(event.target.matches('[data-treatment-theme-search]'))filterPicker(event.target.closest('#treatment-theme-overlay'));});
document.addEventListener('click',async event=>{const button=event.target.closest('button');if(!button)return;if(button.dataset.openTreatmentTheme!==undefined){button.disabled=true;try{const items=await ensureTreatmentThemeLibrary();document.querySelector('#treatment-theme-overlay')?.remove();renderPicker(items);}catch(error){alert(error.message);}finally{button.disabled=false;}return;}if(button.dataset.closeTreatmentTheme!==undefined){document.querySelector('#treatment-theme-overlay')?.remove();return;}if(button.dataset.treatmentThemeFilter!==undefined){const overlay=button.closest('#treatment-theme-overlay');overlay?.querySelectorAll('[data-treatment-theme-filter]').forEach(x=>x.classList.toggle('active',x===button));filterPicker(overlay);return;}if(button.dataset.applyTreatmentTheme){const item=treatmentThemeById(button.dataset.applyTreatmentTheme);document.querySelector('#treatment-theme-overlay')?.remove();applySuggestion(item);}},true);
