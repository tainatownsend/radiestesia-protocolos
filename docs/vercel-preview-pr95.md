# Vercel preview trigger for PR #95

This documentation-only file exists to trigger the first Vercel preview deployment for the `fluxa/ux-architecture-refresh` branch after the project was connected to Vercel. It does not change Fluxa runtime behavior.
